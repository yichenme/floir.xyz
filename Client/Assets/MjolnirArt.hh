#pragma once

#include <Client/Render/Renderer.hh>

namespace MjolnirArt {
    // Draws the hammer centred at the origin. See MjolnirArt.cc for the
    // (auto-generated, do-not-hand-edit) path data this replays.
    void draw(Renderer &ctx, float r);
}
